class hashTable {
    constructor(size) {
        this.size = size;
        this.table = new Array(size)
    }

    hash(key) {
        let total = 0;
        for (let i = 0; i < key.length; i++){
            
        }
    }
}